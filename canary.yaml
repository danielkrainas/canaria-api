version: 0.1

auth:
  silly:
    realm: silly-realm
    service: silly-service

log:
  level: 'debug'
  formatter: 'text'

http:
  addr: ':6789'
  net: 'tcp'
  host: 'localhost:6789'

storage: 'memory'

