version: 0.1

#auth:
#  silly:
#    realm: silly-realm
#    service: silly-service

log:
  level: 'debug'
  formatter: 'text'

http:
  addr: ':6789'
  net: 'tcp'
  host: 'localhost:6789'
  relativeurls: false
  headers:
    x-random: [1]
    x-another: ['something totally else']

storage: 'memory'

